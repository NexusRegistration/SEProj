const express = require('express');
var router = express.Router();
const mongoose = require('mongoose');

router.post('/:classID/edit', (req, res) => {

});

router.post('/:classID/delete', (req, res) => {

});

module.exports = router;